.game-card {
    background: white;
    border-radius: 20rpx;
    padding: 40rpx;
    text-align: center;
    margin-bottom: 30rpx;
    box-shadow: 0 5rpx 15rpx rgba(0,0,0,0.05);
    transition: transform 0.3s;
}
.game-card:active {
    transform: scale(0.98);
}